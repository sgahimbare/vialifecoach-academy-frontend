export default function LecturerQuizRulesInfoPage() {
  return (
    <main className="mx-auto max-w-3xl p-6">
      <h1 className="text-2xl font-semibold">Quiz Rules</h1>
      <p className="mt-2 text-gray-700">
        Learners must acknowledge rules before quiz start. Quiz content blocks copy/cut/right-click and
        answer inputs block paste events.
      </p>
    </main>
  );
}
